# Task 03.4: Align Test Names and Fix Integration Tests

**Status**: [ ] pending
**Dependencies**: Task 03.3
**Wave**: 2
**Complexity**: M

## Pre-Task Gate
- Run: `nix develop --command cargo test -p consensus-simplex -- test_engine_can_start_and_shutdown`
- Expected: exit 0 (Task 03.3 must have wired the real engine)
- If gate fails: **STOP. Task 03.3 is not complete.**

## Context
The original Task 3's Acceptance Criteria reference 3 test names that don't exist because Task 1 created differently-named tests:
- Plan expects `test_engine_starts_with_real_simplex` → actual: `test_engine_start_and_status`
- Plan expects `test_engine_shutdown_aborts_handle` → actual: `test_engine_shutdown`
- Plan expects `test_engine_status_tracks_height` → actual: `test_engine_height_tracking`

Additionally, several engine lifecycle and E2E tests currently hang for 60+ seconds because they were written against the stub engine. With real simplex wiring in place (Task 03.3), these tests need timing adjustments and possibly mock updates.

## What to do
1. **Rename tests** to match the original plan's AC names:
   - `test_engine_start_and_status` → `test_engine_starts_with_real_simplex` (in `tests.rs`)
   - `test_engine_shutdown` → `test_engine_shutdown_aborts_handle` (in `tests.rs`)
   - `test_engine_height_tracking` → `test_engine_status_tracks_height` (in `tests.rs`)
2. **Fix engine lifecycle tests** that hang:
   - `test_engine_can_start_and_shutdown` (engine.rs) — adjust for real simplex startup timing
   - `test_engine_simulates_block_finalization` (engine.rs) — rename if needed (stub simulation is gone), adjust to verify real block finalization
3. **Fix E2E integration tests** if they hang with the real engine:
   - `test_single_validator_produces_block` — may need adjusted timeout or mock network changes
   - `test_single_validator_with_transactions` — same
4. **Verify** the full workspace test suite passes.

## Mock Boundary
**Allowed to mock**: Network provider, consensus app in unit tests.
**Must NOT mock**: The engine startup itself — tests must exercise the real vendor simplex engine.

## Must NOT do
- Do not edit `vendor/`.
- Do not change `crates/consensus/` trait definitions.
- Do not change the engine `start()` implementation (that was Task 03.3).
- Do not drop any test — all existing tests must still exist (possibly renamed).

## References
- `crates/consensus-simplex/src/tests.rs`: All test functions.
- `crates/consensus-simplex/src/engine.rs`: Engine-local test functions.
- `.sisyphus/evidence/real-simplex-consensus-wiring/03-engine-constructor.log`: Evidence of hanging tests.
- `.sisyphus/evidence/real-simplex-consensus-wiring/02-config-extend.log`: Evidence of E2E test hangs.

## Acceptance Criteria
- `nix develop --command cargo test -p consensus-simplex -- test_engine_starts_with_real_simplex test_engine_shutdown_aborts_handle test_engine_status_tracks_height` passes (exit 0).
- `nix develop --command cargo build --workspace && nix develop --command cargo test --workspace` passes (exit 0).
- All renamed tests exercise the real vendor simplex engine (not the removed stub).
- Evidence: `.sisyphus/evidence/task-03.4-align-tests-integration.txt`

## Post-Task Gate
- Run: `nix develop --command cargo build --workspace && nix develop --command cargo test --workspace`
- Expected: exit 0
- Evidence MUST be appended to `.sisyphus/evidence/task-03.4-align-tests-integration.txt`.

## Evidence
- `.sisyphus/evidence/task-03.4-align-tests-integration.txt`
