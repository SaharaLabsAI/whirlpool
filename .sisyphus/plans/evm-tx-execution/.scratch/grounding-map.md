# Codebase Grounding Map — EVM Transaction Execution

> Generated by explore agent + manual verification.
> Grounds task definitions against actual current codebase state.

---

## 1. app-evm/src/executor.rs (PRIMARY CHANGE TARGET)

**EvmApplication struct**: `EvmApplication<DB> { evm_config: WhirlpoolEvmConfig, state_db: Arc<RwLock<DB>>, tx_source: Arc<dyn TxSource> }`

**propose()**: Currently a stub — acquires read lock, builds header from parent, returns empty block with `EMPTY_ROOT_HASH` for tx/receipts roots, 0 gas, empty transactions vec.

**verify()**: Currently a stub — acquires read lock, checks only `state_root` match between proposed and current `state_db.state_root()`. No re-execution.

**Current imports**: `arc`, `alloy_primitives`, app types (Application, EvmBlock, ExecutionResult, TxSource), `alloy_trie::EMPTY_ROOT_HASH`, reth header traits, WhirlpoolEvmConfig, EvmAppError.

**Missing imports needed**: reth-evm (ConfigureEvm, BlockBuilder, BasicBlockExecutor), reth-revm (State), reth-execution-types (BlockExecutionResult), alloy-consensus (TxReceipt), TransactionSigned decode/recover.

## 2. app-evm/Cargo.toml (DEPENDENCIES)

**Already present**: app, state, consensus, reth-evm, reth-revm, alloy-trie, alloy-primitives, alloy-consensus, revm.
**May need to add**: reth-execution-types, reth-primitives-traits (if not already transitive), reth-ethereum-primitives.

## 3. state/src/db.rs (SECONDARY TARGET)

**Clone derives**: ✅ ALREADY PRESENT on both InMemoryStateDb (line 12) and DbAccount (line 18).
**commit()**: `commit(&mut self, bundle: &BundleState)` — processes accounts + bytecodes. Already implemented.
**state_root()**: Flat keccak256 hash, not MPT. Accepted for MVP.

**IMPACT**: Task 1 (add Clone derives) is ALREADY DONE. Remove from plan or convert to verification-only.

## 4. Reth Vendor APIs (CONFIRMED AVAILABLE)

| API | Location | Status |
|-----|----------|--------|
| ConfigureEvm trait | vendor/reth/crates/evm/evm/src/lib.rs | ✅ Available |
| builder_for_next_block | ConfigureEvm method | ✅ Available |
| BlockBuilder trait | vendor/reth/crates/evm/evm/src/execute.rs | ✅ Available |
| BasicBlockExecutor | vendor/reth/crates/evm/evm/src/execute.rs | ✅ Available |
| execute_one | BasicBlockExecutor method | ✅ Available |
| NextBlockEnvAttributes | vendor/reth/crates/evm/evm/src/lib.rs | ✅ Available |
| reth_revm::State | vendor/reth/crates/revm/ | ✅ Available |
| TransactionSigned::decode_2718 | alloy/reth-ethereum-primitives | ✅ Available |

## 5. alloy-trie

External cargo dependency (v0.9), not vendored. `ordered_trie_root_with_encoder` available for tx_root/receipts_root computation.

## 6. app types (READ-ONLY REFERENCE)

**EvmBlock**: Contains header fields (state_root, transactions_root, receipts_root, gas_used, etc.), transactions vec, state_root field.
**ExecutionResult**: Contains execution output data.
**Application trait**: Async propose/verify signatures matching current EvmApplication impl.

## 7. Task Plan Adjustments

Based on grounding:
- **Remove Task 1** (Clone derives already present) → T-12 test still needed (verify clone isolation)
- **Task 3 deps check**: reth-evm, reth-revm, alloy-trie already in Cargo.toml → may only need reth-execution-types, reth-ethereum-primitives
- **Wave 1 now**: Tasks 2, 3 only (state tests + decode helper)
- **Wave 2 unchanged**: Tasks 4, 5 (propose + verify)
- **Wave 3 unchanged**: Tasks 6, 7, 8 (integration + verify + llmdocs)
