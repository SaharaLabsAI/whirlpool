# Task 07: update-llmdocs

## Summary
Update the `llmdocs` documentation for the modified `app-evm` and `state` crates.

## Crate(s)
`app-evm`, `state`

## Files Changed
Documentation files (generated by `ctx-update-doc`)

## Dependencies
Task 06 (workspace-verification)

## Design Refs
`AGENTS.md` Fast rules

## TDD Sequence
1. Run `ctx-update-doc` for `app-evm` and `state` (Action)
2. Verify updated documentation reflects the new execution logic.

## Implementation Details
Use the `ctx-update-doc` skill for both crates.

## Acceptance Criteria
- `ctx-update-doc` skill runs successfully for both `app-evm` and `state` crates
- `nix develop --command cargo doc -p app-evm -p state --no-deps` succeeds (if applicable)
- Generated llmdocs files exist and reflect the new `propose`/`verify` implementations

## Evidence
- Path: `.sisyphus/evidence/evm-tx-execution/07-update-llmdocs.log`
