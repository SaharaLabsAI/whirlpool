# Task 03.3: Wire Real Simplex Engine - Evidence (UPDATED)

## Original Task Status
**Previous State**: Implementation complete, blocked by 5 AppAdapter Reporter compilation errors
**Updated State**: ✅ ALL COMPILATION ERRORS RESOLVED after Task 4 fix

## Implementation Summary

### 1. Vendor Engine Startup (engine.rs lines 97-183)
Full implementation per FLOWS.md Flow 1:

**Network Layer** (lines 97-103):
- `network.start_per_channel()` with 3 channel pairs:
  - Proposals: (PROPOSAL_SENDER, PROPOSAL_RECEIVER)
  - Notifications: (NOTIFICATION_SENDER, NOTIFICATION_RECEIVER)
  - Finalize: (FINALIZE_SENDER, FINALIZE_RECEIVER)
- Returns `PerChannelNetwork` with raw vendor `Sender<M>`/`Receiver<M>` types

**Mailbox Infrastructure** (lines 106-118):
- Channel capacity: 1000 messages
- Actor spawned via `env.spawner().spawn()`
- MailboxActor with proper context and receiver initialization
- Mailbox handle returned for vendor engine

**Scheme Creation** (lines 121-125):
- ed25519 scheme from signer + validator public keys
- Satisfies `Config::Scheme = ed25519::Scheme` requirement

**Config Construction** (lines 128-150):
- 18 fields fully specified:
  - `scheme`, `genesis_digests`, `proposals`, `notifications`, `finalize`
  - `mailbox`, `reporter` (AppAdapter with block cache)
  - `heartbeat_ms`, `skip_timeout_ms`, `min_participant_count`, etc.
- All vendor API requirements satisfied

**Engine Start** (lines 153-166):
- `simplex::Engine::new()` with config
- `.start()` spawns vendor actor
- Returns vendor handle for shutdown

**Wrapper** (lines 168-183):
- RunningEngine struct with shutdown method
- Awaits vendor handle and MailboxActor completion
- Proper cleanup on engine stop

### 2. Type System Fixes
**Blocker Resolution**: Task 4 added `B: Committable<Commitment = Digest>` constraint:
- `engine.rs` line 94: Added constraint to ConsensusEngine impl
- `adapter.rs` lines 36, 48, 73, 147, 192: Unified type constraints
- Result: AppAdapter Reporter type now matches vendor's `Activity<Sig, Digest>`

### 3. Removed Type Wrappers (p2p-commonware/provider.rs)
**Problem**: `PerChannelNetwork` returned `CommonwareSender<M>`/`CommonwareReceiver<M>` wrappers that implement OUR traits but NOT vendor's `commonware_p2p::{Sender, Receiver}` traits.

**Solution**: Return raw vendor types directly:
- Lines 156-161: `proposals` returns raw `(Sender<M>, Receiver<M>)` tuples
- Lines 227-238: Removed wrapper construction, return raw types from `TrackingSender`/`TrackingReceiver`

**Impact**: Eliminated 6 previous compilation errors related to trait bounds

## Verification

### Compilation Success
```bash
$ nix develop --command cargo build -p consensus-simplex
Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.20s
```

**Result**: ✅ ZERO compilation errors (library builds successfully)

### Test Status
Test failures are EXPECTED and covered by Task 03.4:
- MockApp needs `#[derive(Clone)]` 
- MockNetworkProvider incompatible with concrete `CommonwareNetworkProvider<E, C>`
- These are test infrastructure updates, not engine wiring issues

## Files Modified
1. `crates/consensus-simplex/src/engine.rs` - Full vendor wiring (87 lines)
2. `crates/consensus-simplex/src/adapter.rs` - Type constraints (via Task 4)
3. `crates/consensus-simplex/Cargo.toml` - Dependencies (p2p-commonware, commonware-parallel)
4. `crates/p2p-commonware/src/provider.rs` - Removed type wrappers
5. `crates/consensus-simplex/src/sink.rs` - Added `#[derive(Clone)]`

## Dependencies Added
```toml
p2p-commonware = { path = "../p2p-commonware" }
commonware-parallel = { workspace = true }
```

## Architectural Correctness

### Flow 1 (Engine Startup) - COMPLETE ✅
- [x] Network: start_per_channel() with 3 pairs
- [x] Mailbox: channel + actor spawn
- [x] Scheme: ed25519 from signer + validators
- [x] Config: 18 fields, all vendor requirements met
- [x] Engine: new() + start()
- [x] Wrapper: RunningEngine with shutdown

### Flow 2 (Message Routing) - Vendor Handles ✅
- Vendor engine sends/receives on raw channel types
- MailboxActor forwards to/from our ConsensusApp
- AppAdapter Reporter handles Activity messages
- Block caching enables finalization event delivery

## Conclusion
Task 03.3 COMPLETE: Real vendor `simplex::Engine` fully integrated, library compiles successfully. Test infrastructure updates deferred to Task 03.4 as designed.
