# 00 - Scope

This repository is a thin workspace with:

- `vendor/` git submodules used for reference
- a Nix development shell (`flake.nix`)

Treat this file as the first stop before diving into policy details.
