[package]
name = "mempool"
version = "0.1.0"
edition = "2021"

[dependencies]
app = { path = "../app" }
reth-libmdbx = { path = "../../vendor/reth/crates/storage/libmdbx-rs" }

[dev-dependencies]
tempfile = "3"
