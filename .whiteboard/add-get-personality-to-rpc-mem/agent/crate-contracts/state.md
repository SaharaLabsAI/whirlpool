# Crate Contract: state

## Responsibilities
- Provide canonical finalized personality representation (`StoredPersonality`).
- Provide lookup contract via `PersonalityStorage::get_latest`.

## Notes
- Existing API is sufficient for MVP read endpoint.
