# Dependency Digest

- `rpc-mem` already depends on `app-mem` and JSON-RPC stack.
- Read endpoint likely requires explicit use of `state` trait types in service adapter boundary.
- No new external crates expected for MVP read method.
